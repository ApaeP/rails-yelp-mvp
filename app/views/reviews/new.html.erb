</div class="text-center">
  <h2>Review <%= @restaurant.name %></h2>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Send review", class: "btn btn-info" %>
  <% end %>
<div>

<div class="mt-5 text-center">
  <%= link_to 'Go back to Restaurant', restaurant_path(@restaurant), class: "btn btn-info" %>
</div>
